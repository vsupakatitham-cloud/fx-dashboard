# Local tooling — never publish these (large / machine-specific)
node_modules/
.pw-browsers/
.npm-cache/
logs/
.DS_Store

# Local input artifacts
Screenshot*.png

# Quarantined (validation-failed) snapshots — never published
quarantine/
